server:
  port: 9004

spring:
  application:
    name: cloud-nacos-config
  cloud:
    nacos:
      discovery:
        server-addr: 106.14.19.0:8848 # 注册中心
      config:
        file-extension: yaml # 配置中心后缀名
        server-addr: 106.14.19.0:8848 # 配置中心

#${prefix}-${spring.profile.active}.${file-extension}
# ${spring.application.name}-${spring.profile.active}.${file-extension}
# nacos-config-client-dev.yml
# namespace + group + dataId 进行区分